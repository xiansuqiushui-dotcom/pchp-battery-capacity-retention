# BaSyTec 外部计算确认最终预冻结 V352

V352 保留 V343 与 V347 两次结构失败的完整记录，不把它们包装成模型结果。V343 没有找到预期的独立循环容量列；V347 只用一个开发电芯确定表头，未覆盖空白分隔的结构族。两次均没有读取确认容量、计算误差或写出可评分预测。

在本次冻结前，程序对 (47) 个确认文件只读取到表头行并立即停止，共读取 (0) 个数据行。普查发现 (32) 个逗号分隔文件和 (15) 个空白分隔文件；核心语义字段完全一致，附加控制列是否存在不影响模型。这个表头普查属于不含结局的信息检查，不能用于选择模型、容量公式或判定门槛。

`F0001` 仍永久排除。其余 (47) 个电芯保持确认集合，且容量值在本次冻结时仍未打开。

容量恢复、目标、模型、早期窗口、PCHP 的 \(\alpha=0.01\)、无保护比较器的 \(\alpha=0.05\)、伤害预算 \(\delta=0.01\)、电芯等权统计量、(100{,}000) 次电芯 bootstrap、随机种子 (20260808) 以及全部通过门槛均与 V347 完全相同。

循环放电容量继续固定为

\[
Q_c=\sum_{j=2}^{n_c}\mathbf{1}\!\left(I_j<-10^{-5}\,\mathrm{A}\right)
\max\!\left(A_{j-1}-A_j,0\right),
\]

主要效应继续固定为

\[
\Delta=\frac{1}{N}\sum_{c=1}^{N}
\left(\operatorname{MAE}_{c,\mathrm{PCHP}}-
\operatorname{MAE}_{c,\mathrm{baseline}}\right).
\]

少于 (36) 个合格电芯记为 `INCONCLUSIVE`。达到执行样本量后，只有 \(\Delta<0\)、至少三分之二非并列电芯改善、(95\%\) bootstrap 区间上界小于 (0)，且三项伤害检查均不超过 \(0.01+10^{-12}\) 时才记为 `RETAIN`；否则记为 `REJECT`。

冻结后不得根据容量或误差结果更改解析族、目标恢复、模型、窗口、超参数、统计单位或门槛。
